package com.example.bloodlink
import org.junit.Test
import org.junit.Assert.*
import com.example.bloodlink.data.BloodRequest
class BloodRequestGeneratedTest {

    @Test
    fun testBloodRequestAttributes_1() {
        val req = BloodRequest(id="req_0", urgency="Emergency", status="Pending")
        assertEquals("req_0", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_2() {
        val req = BloodRequest(id="req_1", urgency="Normal", status="Pending")
        assertEquals("req_1", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_3() {
        val req = BloodRequest(id="req_2", urgency="Emergency", status="Pending")
        assertEquals("req_2", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_4() {
        val req = BloodRequest(id="req_3", urgency="Normal", status="Pending")
        assertEquals("req_3", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_5() {
        val req = BloodRequest(id="req_4", urgency="Emergency", status="Pending")
        assertEquals("req_4", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_6() {
        val req = BloodRequest(id="req_5", urgency="Normal", status="Pending")
        assertEquals("req_5", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_7() {
        val req = BloodRequest(id="req_6", urgency="Emergency", status="Pending")
        assertEquals("req_6", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_8() {
        val req = BloodRequest(id="req_7", urgency="Normal", status="Pending")
        assertEquals("req_7", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_9() {
        val req = BloodRequest(id="req_8", urgency="Emergency", status="Pending")
        assertEquals("req_8", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_10() {
        val req = BloodRequest(id="req_9", urgency="Normal", status="Pending")
        assertEquals("req_9", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_11() {
        val req = BloodRequest(id="req_10", urgency="Emergency", status="Pending")
        assertEquals("req_10", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_12() {
        val req = BloodRequest(id="req_11", urgency="Normal", status="Pending")
        assertEquals("req_11", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_13() {
        val req = BloodRequest(id="req_12", urgency="Emergency", status="Pending")
        assertEquals("req_12", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_14() {
        val req = BloodRequest(id="req_13", urgency="Normal", status="Pending")
        assertEquals("req_13", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_15() {
        val req = BloodRequest(id="req_14", urgency="Emergency", status="Pending")
        assertEquals("req_14", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_16() {
        val req = BloodRequest(id="req_15", urgency="Normal", status="Pending")
        assertEquals("req_15", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_17() {
        val req = BloodRequest(id="req_16", urgency="Emergency", status="Pending")
        assertEquals("req_16", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_18() {
        val req = BloodRequest(id="req_17", urgency="Normal", status="Pending")
        assertEquals("req_17", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_19() {
        val req = BloodRequest(id="req_18", urgency="Emergency", status="Pending")
        assertEquals("req_18", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_20() {
        val req = BloodRequest(id="req_19", urgency="Normal", status="Pending")
        assertEquals("req_19", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_21() {
        val req = BloodRequest(id="req_20", urgency="Emergency", status="Pending")
        assertEquals("req_20", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_22() {
        val req = BloodRequest(id="req_21", urgency="Normal", status="Pending")
        assertEquals("req_21", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_23() {
        val req = BloodRequest(id="req_22", urgency="Emergency", status="Pending")
        assertEquals("req_22", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_24() {
        val req = BloodRequest(id="req_23", urgency="Normal", status="Pending")
        assertEquals("req_23", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_25() {
        val req = BloodRequest(id="req_24", urgency="Emergency", status="Pending")
        assertEquals("req_24", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_26() {
        val req = BloodRequest(id="req_25", urgency="Normal", status="Pending")
        assertEquals("req_25", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_27() {
        val req = BloodRequest(id="req_26", urgency="Emergency", status="Pending")
        assertEquals("req_26", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_28() {
        val req = BloodRequest(id="req_27", urgency="Normal", status="Pending")
        assertEquals("req_27", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_29() {
        val req = BloodRequest(id="req_28", urgency="Emergency", status="Pending")
        assertEquals("req_28", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_30() {
        val req = BloodRequest(id="req_29", urgency="Normal", status="Pending")
        assertEquals("req_29", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_31() {
        val req = BloodRequest(id="req_30", urgency="Emergency", status="Pending")
        assertEquals("req_30", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_32() {
        val req = BloodRequest(id="req_31", urgency="Normal", status="Pending")
        assertEquals("req_31", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_33() {
        val req = BloodRequest(id="req_32", urgency="Emergency", status="Pending")
        assertEquals("req_32", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_34() {
        val req = BloodRequest(id="req_33", urgency="Normal", status="Pending")
        assertEquals("req_33", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_35() {
        val req = BloodRequest(id="req_34", urgency="Emergency", status="Pending")
        assertEquals("req_34", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_36() {
        val req = BloodRequest(id="req_35", urgency="Normal", status="Pending")
        assertEquals("req_35", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_37() {
        val req = BloodRequest(id="req_36", urgency="Emergency", status="Pending")
        assertEquals("req_36", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_38() {
        val req = BloodRequest(id="req_37", urgency="Normal", status="Pending")
        assertEquals("req_37", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_39() {
        val req = BloodRequest(id="req_38", urgency="Emergency", status="Pending")
        assertEquals("req_38", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_40() {
        val req = BloodRequest(id="req_39", urgency="Normal", status="Pending")
        assertEquals("req_39", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_41() {
        val req = BloodRequest(id="req_40", urgency="Emergency", status="Pending")
        assertEquals("req_40", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_42() {
        val req = BloodRequest(id="req_41", urgency="Normal", status="Pending")
        assertEquals("req_41", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_43() {
        val req = BloodRequest(id="req_42", urgency="Emergency", status="Pending")
        assertEquals("req_42", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_44() {
        val req = BloodRequest(id="req_43", urgency="Normal", status="Pending")
        assertEquals("req_43", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_45() {
        val req = BloodRequest(id="req_44", urgency="Emergency", status="Pending")
        assertEquals("req_44", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_46() {
        val req = BloodRequest(id="req_45", urgency="Normal", status="Pending")
        assertEquals("req_45", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_47() {
        val req = BloodRequest(id="req_46", urgency="Emergency", status="Pending")
        assertEquals("req_46", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_48() {
        val req = BloodRequest(id="req_47", urgency="Normal", status="Pending")
        assertEquals("req_47", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_49() {
        val req = BloodRequest(id="req_48", urgency="Emergency", status="Pending")
        assertEquals("req_48", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_50() {
        val req = BloodRequest(id="req_49", urgency="Normal", status="Pending")
        assertEquals("req_49", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_51() {
        val req = BloodRequest(id="req_50", urgency="Emergency", status="Pending")
        assertEquals("req_50", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_52() {
        val req = BloodRequest(id="req_51", urgency="Normal", status="Pending")
        assertEquals("req_51", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_53() {
        val req = BloodRequest(id="req_52", urgency="Emergency", status="Pending")
        assertEquals("req_52", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_54() {
        val req = BloodRequest(id="req_53", urgency="Normal", status="Pending")
        assertEquals("req_53", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_55() {
        val req = BloodRequest(id="req_54", urgency="Emergency", status="Pending")
        assertEquals("req_54", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_56() {
        val req = BloodRequest(id="req_55", urgency="Normal", status="Pending")
        assertEquals("req_55", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_57() {
        val req = BloodRequest(id="req_56", urgency="Emergency", status="Pending")
        assertEquals("req_56", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_58() {
        val req = BloodRequest(id="req_57", urgency="Normal", status="Pending")
        assertEquals("req_57", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_59() {
        val req = BloodRequest(id="req_58", urgency="Emergency", status="Pending")
        assertEquals("req_58", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_60() {
        val req = BloodRequest(id="req_59", urgency="Normal", status="Pending")
        assertEquals("req_59", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_61() {
        val req = BloodRequest(id="req_60", urgency="Emergency", status="Pending")
        assertEquals("req_60", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_62() {
        val req = BloodRequest(id="req_61", urgency="Normal", status="Pending")
        assertEquals("req_61", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_63() {
        val req = BloodRequest(id="req_62", urgency="Emergency", status="Pending")
        assertEquals("req_62", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_64() {
        val req = BloodRequest(id="req_63", urgency="Normal", status="Pending")
        assertEquals("req_63", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_65() {
        val req = BloodRequest(id="req_64", urgency="Emergency", status="Pending")
        assertEquals("req_64", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_66() {
        val req = BloodRequest(id="req_65", urgency="Normal", status="Pending")
        assertEquals("req_65", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_67() {
        val req = BloodRequest(id="req_66", urgency="Emergency", status="Pending")
        assertEquals("req_66", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_68() {
        val req = BloodRequest(id="req_67", urgency="Normal", status="Pending")
        assertEquals("req_67", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_69() {
        val req = BloodRequest(id="req_68", urgency="Emergency", status="Pending")
        assertEquals("req_68", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_70() {
        val req = BloodRequest(id="req_69", urgency="Normal", status="Pending")
        assertEquals("req_69", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_71() {
        val req = BloodRequest(id="req_70", urgency="Emergency", status="Pending")
        assertEquals("req_70", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_72() {
        val req = BloodRequest(id="req_71", urgency="Normal", status="Pending")
        assertEquals("req_71", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_73() {
        val req = BloodRequest(id="req_72", urgency="Emergency", status="Pending")
        assertEquals("req_72", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_74() {
        val req = BloodRequest(id="req_73", urgency="Normal", status="Pending")
        assertEquals("req_73", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_75() {
        val req = BloodRequest(id="req_74", urgency="Emergency", status="Pending")
        assertEquals("req_74", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_76() {
        val req = BloodRequest(id="req_75", urgency="Normal", status="Pending")
        assertEquals("req_75", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_77() {
        val req = BloodRequest(id="req_76", urgency="Emergency", status="Pending")
        assertEquals("req_76", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_78() {
        val req = BloodRequest(id="req_77", urgency="Normal", status="Pending")
        assertEquals("req_77", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_79() {
        val req = BloodRequest(id="req_78", urgency="Emergency", status="Pending")
        assertEquals("req_78", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_80() {
        val req = BloodRequest(id="req_79", urgency="Normal", status="Pending")
        assertEquals("req_79", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_81() {
        val req = BloodRequest(id="req_80", urgency="Emergency", status="Pending")
        assertEquals("req_80", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_82() {
        val req = BloodRequest(id="req_81", urgency="Normal", status="Pending")
        assertEquals("req_81", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_83() {
        val req = BloodRequest(id="req_82", urgency="Emergency", status="Pending")
        assertEquals("req_82", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_84() {
        val req = BloodRequest(id="req_83", urgency="Normal", status="Pending")
        assertEquals("req_83", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_85() {
        val req = BloodRequest(id="req_84", urgency="Emergency", status="Pending")
        assertEquals("req_84", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_86() {
        val req = BloodRequest(id="req_85", urgency="Normal", status="Pending")
        assertEquals("req_85", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_87() {
        val req = BloodRequest(id="req_86", urgency="Emergency", status="Pending")
        assertEquals("req_86", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_88() {
        val req = BloodRequest(id="req_87", urgency="Normal", status="Pending")
        assertEquals("req_87", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_89() {
        val req = BloodRequest(id="req_88", urgency="Emergency", status="Pending")
        assertEquals("req_88", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_90() {
        val req = BloodRequest(id="req_89", urgency="Normal", status="Pending")
        assertEquals("req_89", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_91() {
        val req = BloodRequest(id="req_90", urgency="Emergency", status="Pending")
        assertEquals("req_90", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_92() {
        val req = BloodRequest(id="req_91", urgency="Normal", status="Pending")
        assertEquals("req_91", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_93() {
        val req = BloodRequest(id="req_92", urgency="Emergency", status="Pending")
        assertEquals("req_92", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_94() {
        val req = BloodRequest(id="req_93", urgency="Normal", status="Pending")
        assertEquals("req_93", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_95() {
        val req = BloodRequest(id="req_94", urgency="Emergency", status="Pending")
        assertEquals("req_94", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_96() {
        val req = BloodRequest(id="req_95", urgency="Normal", status="Pending")
        assertEquals("req_95", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_97() {
        val req = BloodRequest(id="req_96", urgency="Emergency", status="Pending")
        assertEquals("req_96", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_98() {
        val req = BloodRequest(id="req_97", urgency="Normal", status="Pending")
        assertEquals("req_97", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_99() {
        val req = BloodRequest(id="req_98", urgency="Emergency", status="Pending")
        assertEquals("req_98", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_100() {
        val req = BloodRequest(id="req_99", urgency="Normal", status="Pending")
        assertEquals("req_99", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_101() {
        val req = BloodRequest(id="req_100", urgency="Emergency", status="Pending")
        assertEquals("req_100", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_102() {
        val req = BloodRequest(id="req_101", urgency="Normal", status="Pending")
        assertEquals("req_101", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_103() {
        val req = BloodRequest(id="req_102", urgency="Emergency", status="Pending")
        assertEquals("req_102", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_104() {
        val req = BloodRequest(id="req_103", urgency="Normal", status="Pending")
        assertEquals("req_103", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_105() {
        val req = BloodRequest(id="req_104", urgency="Emergency", status="Pending")
        assertEquals("req_104", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_106() {
        val req = BloodRequest(id="req_105", urgency="Normal", status="Pending")
        assertEquals("req_105", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_107() {
        val req = BloodRequest(id="req_106", urgency="Emergency", status="Pending")
        assertEquals("req_106", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_108() {
        val req = BloodRequest(id="req_107", urgency="Normal", status="Pending")
        assertEquals("req_107", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_109() {
        val req = BloodRequest(id="req_108", urgency="Emergency", status="Pending")
        assertEquals("req_108", req.id)
        assertEquals("Emergency", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }

    @Test
    fun testBloodRequestAttributes_110() {
        val req = BloodRequest(id="req_109", urgency="Normal", status="Pending")
        assertEquals("req_109", req.id)
        assertEquals("Normal", req.urgency)
        assertEquals("Pending", req.status)
        assertEquals("1", req.unitsRequired)
    }
}