package com.example.bloodlink
import org.junit.Test
import org.junit.Assert.*
import com.example.bloodlink.data.User
class UserGeneratedTest {

    @Test
    fun testUserAttributes_1() {
        val user = User(uid="uid_0", name="User 0")
        assertEquals("uid_0", user.uid)
        assertEquals("User 0", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_2() {
        val user = User(uid="uid_1", name="User 1")
        assertEquals("uid_1", user.uid)
        assertEquals("User 1", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_3() {
        val user = User(uid="uid_2", name="User 2")
        assertEquals("uid_2", user.uid)
        assertEquals("User 2", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_4() {
        val user = User(uid="uid_3", name="User 3")
        assertEquals("uid_3", user.uid)
        assertEquals("User 3", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_5() {
        val user = User(uid="uid_4", name="User 4")
        assertEquals("uid_4", user.uid)
        assertEquals("User 4", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_6() {
        val user = User(uid="uid_5", name="User 5")
        assertEquals("uid_5", user.uid)
        assertEquals("User 5", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_7() {
        val user = User(uid="uid_6", name="User 6")
        assertEquals("uid_6", user.uid)
        assertEquals("User 6", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_8() {
        val user = User(uid="uid_7", name="User 7")
        assertEquals("uid_7", user.uid)
        assertEquals("User 7", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_9() {
        val user = User(uid="uid_8", name="User 8")
        assertEquals("uid_8", user.uid)
        assertEquals("User 8", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_10() {
        val user = User(uid="uid_9", name="User 9")
        assertEquals("uid_9", user.uid)
        assertEquals("User 9", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_11() {
        val user = User(uid="uid_10", name="User 10")
        assertEquals("uid_10", user.uid)
        assertEquals("User 10", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_12() {
        val user = User(uid="uid_11", name="User 11")
        assertEquals("uid_11", user.uid)
        assertEquals("User 11", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_13() {
        val user = User(uid="uid_12", name="User 12")
        assertEquals("uid_12", user.uid)
        assertEquals("User 12", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_14() {
        val user = User(uid="uid_13", name="User 13")
        assertEquals("uid_13", user.uid)
        assertEquals("User 13", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_15() {
        val user = User(uid="uid_14", name="User 14")
        assertEquals("uid_14", user.uid)
        assertEquals("User 14", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_16() {
        val user = User(uid="uid_15", name="User 15")
        assertEquals("uid_15", user.uid)
        assertEquals("User 15", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_17() {
        val user = User(uid="uid_16", name="User 16")
        assertEquals("uid_16", user.uid)
        assertEquals("User 16", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_18() {
        val user = User(uid="uid_17", name="User 17")
        assertEquals("uid_17", user.uid)
        assertEquals("User 17", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_19() {
        val user = User(uid="uid_18", name="User 18")
        assertEquals("uid_18", user.uid)
        assertEquals("User 18", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_20() {
        val user = User(uid="uid_19", name="User 19")
        assertEquals("uid_19", user.uid)
        assertEquals("User 19", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_21() {
        val user = User(uid="uid_20", name="User 20")
        assertEquals("uid_20", user.uid)
        assertEquals("User 20", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_22() {
        val user = User(uid="uid_21", name="User 21")
        assertEquals("uid_21", user.uid)
        assertEquals("User 21", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_23() {
        val user = User(uid="uid_22", name="User 22")
        assertEquals("uid_22", user.uid)
        assertEquals("User 22", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_24() {
        val user = User(uid="uid_23", name="User 23")
        assertEquals("uid_23", user.uid)
        assertEquals("User 23", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_25() {
        val user = User(uid="uid_24", name="User 24")
        assertEquals("uid_24", user.uid)
        assertEquals("User 24", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_26() {
        val user = User(uid="uid_25", name="User 25")
        assertEquals("uid_25", user.uid)
        assertEquals("User 25", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_27() {
        val user = User(uid="uid_26", name="User 26")
        assertEquals("uid_26", user.uid)
        assertEquals("User 26", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_28() {
        val user = User(uid="uid_27", name="User 27")
        assertEquals("uid_27", user.uid)
        assertEquals("User 27", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_29() {
        val user = User(uid="uid_28", name="User 28")
        assertEquals("uid_28", user.uid)
        assertEquals("User 28", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_30() {
        val user = User(uid="uid_29", name="User 29")
        assertEquals("uid_29", user.uid)
        assertEquals("User 29", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_31() {
        val user = User(uid="uid_30", name="User 30")
        assertEquals("uid_30", user.uid)
        assertEquals("User 30", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_32() {
        val user = User(uid="uid_31", name="User 31")
        assertEquals("uid_31", user.uid)
        assertEquals("User 31", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_33() {
        val user = User(uid="uid_32", name="User 32")
        assertEquals("uid_32", user.uid)
        assertEquals("User 32", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_34() {
        val user = User(uid="uid_33", name="User 33")
        assertEquals("uid_33", user.uid)
        assertEquals("User 33", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_35() {
        val user = User(uid="uid_34", name="User 34")
        assertEquals("uid_34", user.uid)
        assertEquals("User 34", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_36() {
        val user = User(uid="uid_35", name="User 35")
        assertEquals("uid_35", user.uid)
        assertEquals("User 35", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_37() {
        val user = User(uid="uid_36", name="User 36")
        assertEquals("uid_36", user.uid)
        assertEquals("User 36", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_38() {
        val user = User(uid="uid_37", name="User 37")
        assertEquals("uid_37", user.uid)
        assertEquals("User 37", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_39() {
        val user = User(uid="uid_38", name="User 38")
        assertEquals("uid_38", user.uid)
        assertEquals("User 38", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_40() {
        val user = User(uid="uid_39", name="User 39")
        assertEquals("uid_39", user.uid)
        assertEquals("User 39", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_41() {
        val user = User(uid="uid_40", name="User 40")
        assertEquals("uid_40", user.uid)
        assertEquals("User 40", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_42() {
        val user = User(uid="uid_41", name="User 41")
        assertEquals("uid_41", user.uid)
        assertEquals("User 41", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_43() {
        val user = User(uid="uid_42", name="User 42")
        assertEquals("uid_42", user.uid)
        assertEquals("User 42", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_44() {
        val user = User(uid="uid_43", name="User 43")
        assertEquals("uid_43", user.uid)
        assertEquals("User 43", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_45() {
        val user = User(uid="uid_44", name="User 44")
        assertEquals("uid_44", user.uid)
        assertEquals("User 44", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_46() {
        val user = User(uid="uid_45", name="User 45")
        assertEquals("uid_45", user.uid)
        assertEquals("User 45", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_47() {
        val user = User(uid="uid_46", name="User 46")
        assertEquals("uid_46", user.uid)
        assertEquals("User 46", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_48() {
        val user = User(uid="uid_47", name="User 47")
        assertEquals("uid_47", user.uid)
        assertEquals("User 47", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_49() {
        val user = User(uid="uid_48", name="User 48")
        assertEquals("uid_48", user.uid)
        assertEquals("User 48", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_50() {
        val user = User(uid="uid_49", name="User 49")
        assertEquals("uid_49", user.uid)
        assertEquals("User 49", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_51() {
        val user = User(uid="uid_50", name="User 50")
        assertEquals("uid_50", user.uid)
        assertEquals("User 50", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_52() {
        val user = User(uid="uid_51", name="User 51")
        assertEquals("uid_51", user.uid)
        assertEquals("User 51", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_53() {
        val user = User(uid="uid_52", name="User 52")
        assertEquals("uid_52", user.uid)
        assertEquals("User 52", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_54() {
        val user = User(uid="uid_53", name="User 53")
        assertEquals("uid_53", user.uid)
        assertEquals("User 53", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_55() {
        val user = User(uid="uid_54", name="User 54")
        assertEquals("uid_54", user.uid)
        assertEquals("User 54", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_56() {
        val user = User(uid="uid_55", name="User 55")
        assertEquals("uid_55", user.uid)
        assertEquals("User 55", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_57() {
        val user = User(uid="uid_56", name="User 56")
        assertEquals("uid_56", user.uid)
        assertEquals("User 56", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_58() {
        val user = User(uid="uid_57", name="User 57")
        assertEquals("uid_57", user.uid)
        assertEquals("User 57", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_59() {
        val user = User(uid="uid_58", name="User 58")
        assertEquals("uid_58", user.uid)
        assertEquals("User 58", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_60() {
        val user = User(uid="uid_59", name="User 59")
        assertEquals("uid_59", user.uid)
        assertEquals("User 59", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_61() {
        val user = User(uid="uid_60", name="User 60")
        assertEquals("uid_60", user.uid)
        assertEquals("User 60", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_62() {
        val user = User(uid="uid_61", name="User 61")
        assertEquals("uid_61", user.uid)
        assertEquals("User 61", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_63() {
        val user = User(uid="uid_62", name="User 62")
        assertEquals("uid_62", user.uid)
        assertEquals("User 62", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_64() {
        val user = User(uid="uid_63", name="User 63")
        assertEquals("uid_63", user.uid)
        assertEquals("User 63", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_65() {
        val user = User(uid="uid_64", name="User 64")
        assertEquals("uid_64", user.uid)
        assertEquals("User 64", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_66() {
        val user = User(uid="uid_65", name="User 65")
        assertEquals("uid_65", user.uid)
        assertEquals("User 65", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_67() {
        val user = User(uid="uid_66", name="User 66")
        assertEquals("uid_66", user.uid)
        assertEquals("User 66", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_68() {
        val user = User(uid="uid_67", name="User 67")
        assertEquals("uid_67", user.uid)
        assertEquals("User 67", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_69() {
        val user = User(uid="uid_68", name="User 68")
        assertEquals("uid_68", user.uid)
        assertEquals("User 68", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_70() {
        val user = User(uid="uid_69", name="User 69")
        assertEquals("uid_69", user.uid)
        assertEquals("User 69", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_71() {
        val user = User(uid="uid_70", name="User 70")
        assertEquals("uid_70", user.uid)
        assertEquals("User 70", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_72() {
        val user = User(uid="uid_71", name="User 71")
        assertEquals("uid_71", user.uid)
        assertEquals("User 71", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_73() {
        val user = User(uid="uid_72", name="User 72")
        assertEquals("uid_72", user.uid)
        assertEquals("User 72", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_74() {
        val user = User(uid="uid_73", name="User 73")
        assertEquals("uid_73", user.uid)
        assertEquals("User 73", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_75() {
        val user = User(uid="uid_74", name="User 74")
        assertEquals("uid_74", user.uid)
        assertEquals("User 74", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_76() {
        val user = User(uid="uid_75", name="User 75")
        assertEquals("uid_75", user.uid)
        assertEquals("User 75", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_77() {
        val user = User(uid="uid_76", name="User 76")
        assertEquals("uid_76", user.uid)
        assertEquals("User 76", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_78() {
        val user = User(uid="uid_77", name="User 77")
        assertEquals("uid_77", user.uid)
        assertEquals("User 77", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_79() {
        val user = User(uid="uid_78", name="User 78")
        assertEquals("uid_78", user.uid)
        assertEquals("User 78", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_80() {
        val user = User(uid="uid_79", name="User 79")
        assertEquals("uid_79", user.uid)
        assertEquals("User 79", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_81() {
        val user = User(uid="uid_80", name="User 80")
        assertEquals("uid_80", user.uid)
        assertEquals("User 80", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_82() {
        val user = User(uid="uid_81", name="User 81")
        assertEquals("uid_81", user.uid)
        assertEquals("User 81", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_83() {
        val user = User(uid="uid_82", name="User 82")
        assertEquals("uid_82", user.uid)
        assertEquals("User 82", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_84() {
        val user = User(uid="uid_83", name="User 83")
        assertEquals("uid_83", user.uid)
        assertEquals("User 83", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_85() {
        val user = User(uid="uid_84", name="User 84")
        assertEquals("uid_84", user.uid)
        assertEquals("User 84", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_86() {
        val user = User(uid="uid_85", name="User 85")
        assertEquals("uid_85", user.uid)
        assertEquals("User 85", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_87() {
        val user = User(uid="uid_86", name="User 86")
        assertEquals("uid_86", user.uid)
        assertEquals("User 86", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_88() {
        val user = User(uid="uid_87", name="User 87")
        assertEquals("uid_87", user.uid)
        assertEquals("User 87", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_89() {
        val user = User(uid="uid_88", name="User 88")
        assertEquals("uid_88", user.uid)
        assertEquals("User 88", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_90() {
        val user = User(uid="uid_89", name="User 89")
        assertEquals("uid_89", user.uid)
        assertEquals("User 89", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_91() {
        val user = User(uid="uid_90", name="User 90")
        assertEquals("uid_90", user.uid)
        assertEquals("User 90", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_92() {
        val user = User(uid="uid_91", name="User 91")
        assertEquals("uid_91", user.uid)
        assertEquals("User 91", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_93() {
        val user = User(uid="uid_92", name="User 92")
        assertEquals("uid_92", user.uid)
        assertEquals("User 92", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_94() {
        val user = User(uid="uid_93", name="User 93")
        assertEquals("uid_93", user.uid)
        assertEquals("User 93", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_95() {
        val user = User(uid="uid_94", name="User 94")
        assertEquals("uid_94", user.uid)
        assertEquals("User 94", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_96() {
        val user = User(uid="uid_95", name="User 95")
        assertEquals("uid_95", user.uid)
        assertEquals("User 95", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_97() {
        val user = User(uid="uid_96", name="User 96")
        assertEquals("uid_96", user.uid)
        assertEquals("User 96", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_98() {
        val user = User(uid="uid_97", name="User 97")
        assertEquals("uid_97", user.uid)
        assertEquals("User 97", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_99() {
        val user = User(uid="uid_98", name="User 98")
        assertEquals("uid_98", user.uid)
        assertEquals("User 98", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_100() {
        val user = User(uid="uid_99", name="User 99")
        assertEquals("uid_99", user.uid)
        assertEquals("User 99", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_101() {
        val user = User(uid="uid_100", name="User 100")
        assertEquals("uid_100", user.uid)
        assertEquals("User 100", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_102() {
        val user = User(uid="uid_101", name="User 101")
        assertEquals("uid_101", user.uid)
        assertEquals("User 101", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_103() {
        val user = User(uid="uid_102", name="User 102")
        assertEquals("uid_102", user.uid)
        assertEquals("User 102", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_104() {
        val user = User(uid="uid_103", name="User 103")
        assertEquals("uid_103", user.uid)
        assertEquals("User 103", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_105() {
        val user = User(uid="uid_104", name="User 104")
        assertEquals("uid_104", user.uid)
        assertEquals("User 104", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_106() {
        val user = User(uid="uid_105", name="User 105")
        assertEquals("uid_105", user.uid)
        assertEquals("User 105", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_107() {
        val user = User(uid="uid_106", name="User 106")
        assertEquals("uid_106", user.uid)
        assertEquals("User 106", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_108() {
        val user = User(uid="uid_107", name="User 107")
        assertEquals("uid_107", user.uid)
        assertEquals("User 107", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_109() {
        val user = User(uid="uid_108", name="User 108")
        assertEquals("uid_108", user.uid)
        assertEquals("User 108", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }

    @Test
    fun testUserAttributes_110() {
        val user = User(uid="uid_109", name="User 109")
        assertEquals("uid_109", user.uid)
        assertEquals("User 109", user.name)
        assertFalse(user.isDonor)
        assertFalse(user.isAdmin)
    }
}