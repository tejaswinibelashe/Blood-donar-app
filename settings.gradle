pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "BloodDonerApp"

include ':app'
project(':app').projectDir = new File('android-native/app')

include ':android-wrapper'
